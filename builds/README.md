# Builds

This folder contains compiled APK/AAB files for distribution.

## Current Builds

- `tempo-flow-debug.apk` - Debug build for testing (4.49 MB)

## Building

To create a new build:

```bash
npm run cap:build:android
```

The APK will be generated in `android/app/build/outputs/apk/debug/`.
